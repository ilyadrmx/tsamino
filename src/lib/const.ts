/** AminoApps API response status codes */
export class StatusCodes {
    static readonly SUCCESS = 0;
    static readonly FORBIDDEN = 403;
}

/** AminoApps websocket message types */
export class SocketMessageTypes {
    static readonly CHAT = 1000;
}

/** AminoApps websocket chat message types */
export class ChatMessageTypes {
    static readonly DEFAULT = 0;
}